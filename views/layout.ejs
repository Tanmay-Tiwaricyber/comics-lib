<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title><%= title || 'Adult Doodle Comics' %></title>
  <link rel="stylesheet" href="/css/style.css">
  <link href="https://fonts.googleapis.com/css2?family=Comic+Neue:wght@400;700&display=swap" rel="stylesheet">
  <%- style %> <!-- This will inject page-specific styles if any -->
</head>
<body>
  <div class="container">
    <header class="doodle-header">
      <h1>Adult Doodle Comics</h1>
      <nav>
        <a href="/">Library</a>
        <% if (user) { %>
          <a href="/logout">Logout (Welcome, <%= user.username %>)</a>
        <% } else { %>
          <a href="/login">Login</a> | <a href="/register">Register</a>
        <% } %>
      </nav>
    </header>
    
    <main>
      <%- body %>
    </main>
    
    <footer class="doodle-footer">
      <p>© <%= new Date().getFullYear() %> Adult Doodle Comics - For mature audiences only</p>
    </footer>
  </div>
  <%- script %> <!-- This will inject page-specific scripts if any -->
  <script src="/js/main.js"></script>
</body>
</html>